import {numberField} from './logging.mjs';
export function workoutPayload(items){if(!items.some(x=>x.is_enabled))throw new Error('Keep at least one exercise enabled.');if(new Set(items.map(x=>x.exercise_id)).size!==items.length)throw new Error('Use each exercise only once.');return items.map((x,i)=>{
 const hold=x.hold_seconds!==''&&x.hold_seconds!=null;
 const result={id:x.id,exercise_id:x.exercise_id,order_index:i,sets:numberField(x.sets,'sets',{min:1,max:20,integer:true,required:true}),rep_min:hold?null:numberField(x.rep_min,'minimum reps',{min:1,max:100,integer:true,required:true}),rep_max:hold?null:numberField(x.rep_max,'maximum reps',{min:1,max:100,integer:true,required:true}),hold_seconds:hold?numberField(x.hold_seconds,'hold seconds',{min:1,max:600,integer:true,required:true}):null,rest_seconds:numberField(x.rest_seconds,'rest seconds',{min:0,max:1200,integer:true,required:true}),target_weight_kg:numberField(x.target_weight_kg,'target load',{max:1000}),is_enabled:!!x.is_enabled,disabled_reason:x.is_enabled?null:x.disabled_reason?.trim()};
 if(!result.is_enabled&&!result.disabled_reason)throw new Error('Give a reason for each disabled exercise.');if(!hold&&result.rep_max<result.rep_min)throw new Error('Maximum reps must be at least minimum reps.');return result;
});}
